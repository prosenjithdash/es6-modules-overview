const add = (first, second) => {
    return first + second;
}

const mul = (first, second) => {
    return first * second;
}
    
const div = ( first, second ) => {
    return first / second;
}
export  {add,mul,div};